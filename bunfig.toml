# Bun configuration for Percy CLI monorepo
[install]
# Use production installs by default in CI
production = false

# Cache configuration
cache = true

# Lockfile configuration
# Note: Bun 1.3+ uses bun.lock (text format) by default
lockfile = "bun.lock"

